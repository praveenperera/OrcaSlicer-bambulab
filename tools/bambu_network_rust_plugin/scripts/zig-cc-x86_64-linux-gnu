#!/usr/bin/env bash
set -euo pipefail

args=()
skip_next=0

for arg in "$@"; do
    if [ "$skip_next" -eq 1 ]; then
        skip_next=0
        continue
    fi

    case "$arg" in
        --target=x86_64-unknown-linux-gnu)
            ;;
        -target)
            skip_next=1
            ;;
        *)
            args+=("$arg")
            ;;
    esac
done

exec zig cc -target x86_64-linux-gnu "${args[@]}"
